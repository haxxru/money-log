# TODO-DONE

- `ui-demo` 전역 스타일 토큰/레이아웃을 앱 전반에 반영
- 헤더/사이드바/대시보드/거래내역/예산/분석/목표 화면 구현 및 라우팅 연결
- Supabase Auth 로그인/회원가입/로그아웃 서버 액션 연결
- 회원가입에 닉네임/초기 잔액 입력 및 메타데이터 저장 구현
- 설정 탭(`/settings`)에서 정기 급여(월급일/월급액) 수정 기능 구현
- 공통 데이터 레이어(`lib/ledger.ts`) 확장 및 사용자 보호 로직(`requireUser`) 적용
- 거래 추가/삭제 및 월별 합계/캘린더형 월 뷰 연동
- 예산 업서트 및 카테고리별 사용률 표시 구현
- 분석 차트(현금흐름 추세, 카테고리 비중) 구현
- 목표 추가/진행률 수정/삭제 구현
- 월급 자동 입금 보정 로직(`ensureCurrentMonthSalary`) 구현
- `expenses.description` 파싱 의존 제거, `tx_type/category` 정규 컬럼 기반으로 기능 전환
- 구 스키마(`expenses_amount_check`, `tx_type/category` 미존재) 호환 fallback 로직 구현
- 대시보드 거래 추가/수정/삭제 다이얼로그 DB 저장 연결
- 거래내역 탭 행 단위 수정/삭제 연결
- 대시보드 KPI `지난달 대비`를 실데이터 기반 동적 계산으로 전환
- 신규 계정(지난달 데이터 없음)에서 `지난달 대비`를 `데이터 없음`으로 표시
- 거래 데이터 CSV/XLSX 내보내기 API 및 파일 가져오기(import) 구현
- 문서 체계(아키텍처/도메인/TODO/기록/컨텍스트) 현재 코드 기준으로 정합성 재정리
